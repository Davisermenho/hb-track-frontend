# AGENTS.md — HB TRACK

Primary instructions live in: `agent.md` (root). Follow it.

Setup (frontend):
- Install: `npm install`
- Dev: `npm run dev` (or `npm run dev:turbo`)
- Lint: `npm run lint`
- Build: `npm run build`
- E2E: `npm run test:e2e`

Non-negotiables:
- discover-before-create
- v2 canonical directories
- no-any-by-default
- respect Next server/client boundaries